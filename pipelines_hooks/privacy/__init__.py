"""tracked-privacy: no host-specific data, local identity or model identity in tracked artifacts.

Identifiers are derived in memory from the current account, home, checkout and
host (or declared through ``AGENT_UTILITIES_PRIVACY_IDENTIFIERS``). Findings
report only ``file:line`` and a category; the matched value is never printed.
"""
